name: lookmix_app
description: Lookmix Flutter App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # ✅ แก้เป็นดึงจากเว็บเวอร์ชันล่าสุดที่พี่อัพขึ้นไป
  lookmix_design_system: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  github_pages: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img.png
    - assets/1.1.png
    - assets/1.2.png
    - assets/1.3.png
    - assets/1.4.png
    - assets/2.1.png
    - assets/2.2.png
    - assets/2.3.png
    - assets/2.4.png